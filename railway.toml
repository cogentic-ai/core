[build]
builder = "ubuntu"
buildCommand = "apt-get update && apt-get install -y unzip && curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install"

[deploy]
startCommand = "~/.bun/bin/bun run src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 100
